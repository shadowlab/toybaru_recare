services:
  toybaru:
    build: .
    ports:
      - "8099:8099"
    volumes:
      - toybaru-data:/data
    environment:
      - TOYBARU_DATA_DIR=/data
    restart: unless-stopped
    dns:
      - 8.8.8.8
      - 8.8.4.4

volumes:
  toybaru-data:
